# install purge remove
```
sudo apt purge -y cheese gnome-calculator gnome-mies thunderbird rhythmbox ~libreoffice gnome-calender gnome-mahjongg aisleriot gnome-sudoku gnome-text-editor shotwell totem
```
```
sudo snap remove firefox snap-store
```
```
sudo apt install net-tools git gh ibus-mozc curl gnome-tweaks tmux texlive-full ncal
```
```
sudo snap install nvim --edge --calssic
```